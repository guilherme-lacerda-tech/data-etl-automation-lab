[project]
name = "data-etl-automation-lab"
version = "0.1.0"
description = "Synthetic ETL automation lab with CSV, JSON, SQLite, reports and manifests."
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
